<% if notice.present? %>
  <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice">
    <%= notice %>
  </p>
<% end %>

<div class="space-y-6">
  <h1 class="text-2xl font-bold">Tasks Manager</h1>

  <%= render "form" %>

  <ul id="task_list" class="flex flex-col space-y-2">
    <%= render @tasks %>
  </ul>
<div>
